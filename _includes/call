{% if include.file != "" or include.file != null %}
    {% assign type_arg = {{include.type}} %}
    {% include content/module/{{ include.file | split: ".html" }} type=type_arg %}
{% else %}
    <b>Can't call the file {{include.file}}</b>
{% endif %}